import React from "react";
import type { PostProps } from "@/shared/types";
import { getFieldsRepeater } from "@/shared/lib/meta-fields";

export const NewspaperArchive = ({ title, content, meta }: PostProps) => {
  // console.log(meta);
  console.log(content);

  return <>123</>;
};
